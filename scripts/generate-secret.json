{
    "name": "generate-cron-secret",
    "version": "1.0.0",
    "description": "Generate secure cron secret",
    "scripts": {
        "generate-secret": "node -e \"console.log('\\\\n🔐 Your CRON_SECRET:\\\\n'); console.log(require('crypto').randomBytes(32).toString('hex')); console.log('\\\\nAdd this to your .env.local and Vercel environment variables\\\\n');\""
    }
}